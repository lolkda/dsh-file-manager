# DSH 文件管理器 V1 行为规格

本规格来自本会话已批准的 SDD 讨论及落地计划。实施顺序不改变完整 V1 的验收范围。

## 目标与范围

独立文件主面板，无需打开 Session。管理当前 Linux DSH Host 上的普通目录和文件（包括已挂载目录），支持显式添加额外根、新建/重命名/复制/移动/永久删除、批量任务、文本编辑、文件与目录上传下载、外部变化刷新、引用到指定会话草稿。

不提供回收站、文件搜索、目录合并、断点续传、Git/完整 IDE、新增远程连接协议或多用户 ACL。

## 行为规则

- **R1 根授权**：profile 级保存根目录；只有添加根接受绝对路径。添加目录输入默认固定为 `/`，不自动填入某个 workspace 路径，也不覆盖用户已输入的内容；默认值不等于自动授权，仍须点击添加。其余调用使用 `rootId + relativePath`，读写两端均在 Host 校验。浏览/额外根不会改变 Agent cwd、沙箱或工具权限。
- **R2 根身份**：记录实际目录身份。根失联、被替换或撤销后不能自动操作同路径下的新内容。左侧“移除入口”须先确认并明确不删除磁盘数据，取消不撤销授权；根本身不能递归删除。
- **R3 路径边界**：拒绝非法路径、父目录穿越。Linux provider 通过持有的目录描述符逐层访问，并拒绝跟随链接。链接本身可重命名/删除；特殊条目不静默包含在复制/打包中。
- **R4 读取与编辑**：稳定、有界 UTF-8 快照带版本。默认编辑 5 MiB，保留 BOM、换行及普通 POSIX mode。未知编码/二进制/超限不自动转换。保存需要 expectedVersion，检测到外部修改时拒绝覆盖并保留草稿。
- **R5 草稿**：切换主面板保留未保存内容；关闭脏文档须保存/放弃/取消。刷新不能覆盖脏文档；外部删除只标记失效，不自动重建文件。
- **R6 操作冲突**：文件重名须选择跳过/改名/覆盖，覆盖核对目标版本；目录重名只跳过或改名，不隐式合并。拒绝移动到自身子树，去重祖孙重叠选择。
- **R7 永久删除**：点击后立即显示准备/预览对话框，清单准备过程和失败必须可见并可取消；清单就绪且人工明确勾选不可恢复并确认后才可提交删除。取消不改动数据，迟到的准备结果不能重开对话框或触发删除。旧清单不能无条件删除新出现或已变更的内容。
- **R8 批量任务**：逐项记录成功、失败、未执行；可取消、重试失败项，不承诺整体回滚。跨卷移动须复制、校验、发布后再删源；删源失败明确报告两份仍存在。
- **R9 传输**：目录上传保留层级和空目录；目录下载流式 ZIP，上传 ZIP 不解压。默认并发 2，单文件 2 GiB，单任务 10 GiB/10,000 条目，可配置。中断/限额/空间不足清理未提交暂存，不能报告伪成功。
- **R10 刷新**：覆盖 Agent、终端和外部程序变化，包括原子替换。事件触发失效并重读；监测异常和断线明确显示，恢复时重同步。监听/流/计时器有完整释放路径。
- **R11 引用**：选择目标会话，使用 revision 保护的输入追加，不重建整份草稿、不损坏原有引用、不自动发送或提高 Agent 权限。目标失效/忙碌须明确失败或重试。
- **R12 发布**：通过安装 bundle 接入现有 GUI，不替换应用根或官方 preset。禁用/卸载不删除用户数据，也不能撤销已确认的永久删除。
- **R13 任务历史**：复制/移动/上传/下载记录默认保留，失败不自动消失。完全停止且已完成收尾的记录可单独关闭或批量“清理已结束”；排队、运行及正在收尾/重试接受中的任务不能关闭。关闭只在当前 profile 中持久化隐藏记录，不删除磁盘内容、操作结果或必要恢复证明；刷新/重启不复活同一轮已关闭记录。新的重试重新显示，旧关闭动作不能隐藏新执行。批量按点击时的候选逐项处理，失败项保留并显示原因，不伪称全部成功。折叠只收起任务区，保留标题与活动数，不取消运行或监听。

## 并发与持久性边界

本版提供有界快照、提交前检查、同目标协调及原子发布，不声称对不协作的外部进程具有全局文件锁或任意瞬时竞争下的内核级 CAS。批量目录读取/ZIP 不是文件系统事务快照。已知的不确定状态必须报告，不能为了通过测试而弱化这些规则。

## 验收

1. 无 Session 可添加/浏览根；移除不删文件；根可跨刷新/重启保存。
2. 越界、链接绕行、根替换和无权限请求被拒绝；Agent 权限不变。
3. Unicode/BOM/换行与有界读取；外部版本冲突保留磁盘和草稿。
4. 删除取消/清单过期/目标变化；移动自子树、祖孙选择与同名冲突。
5. 批量部分失败、EXDEV、发布成功但删源失败、取消和幂等重试。
6. 目录传输层级、空目录、ZIP 内容、中断、限额与暂存清理；真实 Connection HTTP 桥接下，已认证且无 Origin 的原生 GET 返回正确文件字节/ZIP并完成任务，未认证或跨站请求仍被拒绝。请求体缓冲模式不能使响应文件全量缓存。
   文本读取使用现有控制接口 `text.read`；大文本保存继续使用流式 POST 请求，不能为回避 GET 构造错误而放宽权限或取消资源限制。
7. 外部程序变更及断线重连刷新；脏文档不被自动更新替换。
8. 引用进入正确会话且只修改草稿；忙碌/失效不误插。
9. 明暗主题、键盘操作、中文文件名及插件卸载后资源释放。
10. 终态卡片单独关闭/批量清理后刷新不复活，文件与恢复证明保留；运行与收尾中的任务不受清理影响，失败持久化不隐藏卡片。折叠不停止工作；迟到快照和旧关闭操作不能隐藏新的重试。

## 当前已验证进度

- P0：24 项自动测试及 JavaScript 语法检查通过。
- P0：bundle 已安装，Host 激活结果为 `applied`；Client 的独立主面板和侧栏入口已注册。
- P0：用户已在现有 GUI 确认“能打开、浏览目录并预览文本”。
- V1 0.1.0 源码：P1–P4 已接入，完整套件 **235/235 通过**，语法检查及原生助手构建通过。
- R4：UTF-8、BOM、CR/LF/CRLF/混合换行、脏草稿与外部冲突均已覆盖；复杂映射超预算明确失败并保留草稿。
- R6：独立两个 manager 的真实竞争曾复现源数据丢失，现无覆盖 rename/目录发布使用 Linux `renameat2(RENAME_NOREPLACE)`；不支持时拒绝，不作不安全回退。
- R7/R8：覆盖和清理采用内容强版本；跨卷删源逐项通过 `beforeRemove` 重证目标及父身份链。live/retry/restore 期间目标消失、同尺寸修改和父路径替换均有真实 EXDEV 回归。
- R9/R10/R11：上传/下载、实际 ZIP、OS watch/SSE、真实 React 组件与 revision 保护的指定会话引用均已做自动测试。
- P5 运行时回归：用户重启 0.1.0 后发生 `invalid unit name 'local-file-manager'`，暴露原测试缺少真实 Storage 命名契约。0.1.0 已撤回并停用。
- 0.1.1：Settings kebab-case 与 Storage snake_case 分离；真实 JSON/Domain 后端的根持久化、操作恢复、Host 挂载、冷重开及失败隔离回归通过，完整套件 **239/239**。存储初始化异常只使文件管理器不可用，保留数据、不开放写操作，不再抛出以阻断整个 Host。
- 0.1.1 已安装保存并启用，打包快照及真实安装目录都通过 4 项真实后端回归。用户随后提供的真实页面截图确认新版主面板、目录列表、实时监测和新建/重命名弹窗已显示，同时暴露了重复选择文案与 Portal 输入框样式问题。
- 0.1.2：文件行只显示一份文件名，复选框保留 `aria-label` 和多选；Portal 表单使用公共 Input 及独立内容样式作用域。新增 5 项 UI 回归，完整套件 **244/244**。用户后续截图确认列表重复文案已消失，同时暴露下载、默认路径与删除反馈问题。
- 0.1.3：默认输入固定 `/`，新增根移除确认，永久删除立即显示准备状态并通过 abort/attemptId 防止迟到或旧回调误删。文本快照统一控制 POST；下载 GET 的请求体模式改为 buffered，响应仍流式。新增 11 项前端及 4 项真实 HTTP 回归，完整套件 **259/259**；实际安装的旧 0.1.2 模块已复现 GET-body 500，修复源码经真实 Cookie/Connection/JSON 后端验证文件、ZIP和鉴权边界。
- 0.1.3 快照通过 63 项 Client/HTTP/Storage 回归，已安装保存并启用，安装器返回 `restart-required`。实际安装的生产模块另通过 4 项真实 HTTP 与 4 项 Storage 回归；本次 UI 的实际页面验收仍待重启刷新后确认。
- 0.1.4：按 R13 增加单条关闭、清理已结束与折叠，关闭标记持久化且保留私有恢复信息；旧关闭操作和迟到快照不得隐藏新重试。新增 67 项回归，完整套件 **326/326** 通过，语法检查通过；其中包含真实 HTTP/JSON 冷重开与 fresh Client 组件闭环，不替代新代次的页面验收。
- 0.1.4 已安装保存并启用，安装器返回 `restart-required`；安装目录的生产模块另通过 **58 项**任务历史/API/真实 HTTP/JSON 回归。客户端文件与测试源码一致，但当前 GUI 新代次的显示仍须重启刷新后核对。
- 尚未进行真实 2 GiB/10,000 条目压力测试。

本发布包的原生助手已针对当前 Linux x64 Host 构建，源码随包提供。完整空目录上传依赖浏览器的目录句柄选择器；文件清单降级无法发现空目录并明确提示。

React 组件测试不替代真实页面验收；当前没有浏览器自动化工具。最终界面检查使用现有 http://127.0.0.1:3080 页面及用户提供的操作结果/截图。
