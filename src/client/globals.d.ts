/**
 * Injected by the build from `package.json`, so the Client can never report a
 * version that disagrees with the package it was built from. See `scripts/build.mjs`.
 */
declare const __FM_VERSION__: string;
